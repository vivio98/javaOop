package encapsule;

public class AverageVOStep1 {
	String name;
	int kor;
	int eng;
	int tot;
	double avg;
	/*
	 * 앞서 절차지향에서 코딩을 하다보니까
	 * 프로그래머들이 어떤 규칙을 발견합니다.
	 * 사람이름 + 과목명 으로 변수명을 주는데
	 * 과목명은 반복되더라 이겁니다.
	 * 따라서 반복되는 과목명을 따로 빼서
	 * 클래스를 만듭니다.
	 * 이때 클래스 바로 밑에(즉 메소드 밑에 영역이 아닌)
	 * 클래스 영역에서 선언된 변수를 인스턴스 변수라고 합니다.
	 * 그리고 인스턴스 변수가 선언된 영역을 "필드" 라고 합니다.
	 * 그러면 오더를 받을 때 "필드에 변수를 선언해라 !?..."라고
	 * 받으시면 지역변수가 아닌 공용으로 쓸 인스턴스 변수를 선언하라는 뜻이 됩니다.
	 * 아주 중요합니다. 꼭 알아두시길!!!! 
	 */
	

}
